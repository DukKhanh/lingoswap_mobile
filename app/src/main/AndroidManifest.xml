<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lingoswap">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".LingoSwapApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LingoSwap">

        <!-- Entry point -->
        <activity
            android:name=".activities.SignInActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SignUpActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.ForgotPasswordActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.ResetPasswordActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.ResetSuccessActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"/>

        <activity
            android:name=".activities.HomeActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"/>

        <activity
            android:name=".activities.MatchingActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.VideoCallActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:keepScreenOn="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LingoSwap.VideoCall"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.RatingActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.ProfileActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.friends.FriendsActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.chat.ChatActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.friends.MatchHistoryActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.chat.ConversationListActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.notification.NotificationListActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.report.ReportActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".activities.IncomingCallActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.OutgoingCallActivity"
            android:configChanges="keyboard|keyboardHidden|uiMode|density"
            android:screenOrientation="portrait"/>

        <!-- Lưu ngôn ngữ per-app cho API < 33 (AppCompatDelegate.setApplicationLocales) -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

    </application>
</manifest>
